// 存储 key 迁移逻辑 - 已废弃
// 旧格式已清理，不再需要迁移

/**
 * @deprecated 旧格式已清理，不再需要迁移
 */
export function migrateStorageKeys(): void {
  // 旧格式已清理，不再需要迁移
  // 此函数保留为空，避免调用处报错
}

/**
 * @deprecated 旧格式已清理，不再需要迁移
 */
export function needsMigration(): boolean {
  return false;
}
